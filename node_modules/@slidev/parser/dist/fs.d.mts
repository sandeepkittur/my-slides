import { a as TimesplitOutput, i as TimesplitInput, n as parseRangeString, o as parseTimesplits, r as parseTimeString, t as parseAspectRatio } from "./utils-DpcPp-I0.mjs";
import { SlidevParserOptions, detectFeatures, extractImagesUsage, getDefaultConfig, parse, parseSlide, parseSync, prettify, prettifySlide, resolveConfig, resolveFonts, stringify, stringifySlide, verifyConfig } from "./core.mjs";
import { PreparserExtensionLoader, SlidevData, SlidevMarkdown } from "@slidev/types";
//#region src/fs.d.ts
/**
 * Whether `filePath` resolves inside any of `roots` (no `..` escape).
 * Inlined here (rather than imported from the `slidev` package) because
 * `@slidev/parser` must stay independent of `@slidev/slidev`.
 */
declare function isPathInsideRoots(filePath: string, roots: string[]): boolean;
declare function injectPreparserExtensionLoader(fn: PreparserExtensionLoader): void;
interface LoadRootsInfo {
  roots: string[];
  userRoot: string;
  /**
   * When provided, `src:` includes resolving outside these roots are
   * rejected (recorded as an error) instead of being loaded. Optional for
   * backward compatibility with other `@slidev/parser` consumers.
   */
  allowedRoots?: string[];
}
/**
 * Slidev data without config and themeMeta,
 * because config and themeMeta depends on the theme to be loaded.
 */
type LoadedSlidevData = Omit<SlidevData, 'config' | 'themeMeta'>;
declare function load(options: LoadRootsInfo, filepath: string, sources?: Record<string, string> | ((path: string) => Promise<string>), mode?: string): Promise<LoadedSlidevData>;
declare function save(markdown: SlidevMarkdown): Promise<string>;
//#endregion
export { LoadRootsInfo, LoadedSlidevData, SlidevParserOptions, TimesplitInput, TimesplitOutput, detectFeatures, extractImagesUsage, getDefaultConfig, injectPreparserExtensionLoader, isPathInsideRoots, load, parse, parseAspectRatio, parseRangeString, parseSlide, parseSync, parseTimeString, parseTimesplits, prettify, prettifySlide, resolveConfig, resolveFonts, save, stringify, stringifySlide, verifyConfig };